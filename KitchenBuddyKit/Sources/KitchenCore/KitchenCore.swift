/// Kitchen Buddy's pure domain layer. See `docs/decisions/` and
/// `.kiro/specs/kitchen-buddy-ios/design.md` for the architecture.
public enum KitchenCore {
    /// Version of the on-disk/JSON export format this build reads and writes.
    public static let exportFormatVersion = "2.0"
}
